<h1>Editing role</h1>

<%= render 'form' %>

<%= link_to 'Show', @role %> |
<%= link_to 'Back', roles_path %>
<br /> <br />
<h2>Users with the role</h2>
<ul>
  <% @users.each do |u| %>
    <li><%= link_to(u.name, user_path(u)) %></li>
  <% end %>
</ul>
